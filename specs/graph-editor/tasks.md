# Tasks: graph-editor (FEAT-004)

| Task | Description | Requirements |
| :--- | :--- | :--- |
| **T1** | Implement the `HarnessWriter` service in the Extension Host. | R4 | [x]
| **T2** | Create logic to update `.agents/agentic.json` with new/deleted agents. | R5, R8, R12 | [x]
| **T3** | Create logic to generate/update `SUBAGENT.md` and `SKILL.md` from templates. | R5, R6, R9 | [x]
| **T4** | Add "Add Node" UI controls to the Webview. | R1 | [x]
| **T5** | Add "Delete Node" UI controls to the nodes or detail panel. | R2, R12 | [x]
| **T6** | Implement `onConnect` in `WhiteboardCanvas` to persist new edges. | R3, R7, R11 | [x]
| **T7** | Turn the Detail Panel into an editable form for missions and descriptions. | R9 | [x]
| **T8** | Implement basic validation (e.g., name uniqueness, no circular deps). | R11 | [x]
| **T9** | Run `./check.sh` and verify persistence across restarts. | R1-R13 | [x]
| **T10** | Add integration tests for the Write-back logic. | R4, R13 | [x]

